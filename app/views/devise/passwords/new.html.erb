<div class="layout">
  <div class="wrap">
    <div class="top_margin clearfix">

     <div class="form-modal">  
        <div class="form-toggle">
           <button id="login-toggle" onclick="toggleLogin()">비밀번호를 잊으셨나요?</button>
        </div>
          
        <div id="login-form">
         <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
          <%= f.error_notification %>
            <div class="login_input">    
              <%= f.input :email, required: true, autofocus: true %>
            </div>
            <%= f.button :submit, t(".send_me_reset_password_instructions"), class: "btn login" %>
            <p><%= render "devise/shared/links" %></p>
         <% end %>
        </div>
      </div>

    </div>
  </div>
</div>


<!-- <h2><%= t(".forgot_your_password") %></h2>
<%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= f.error_notification %>
  <div class="form-inputs">
    <%= f.input :email, required: true, autofocus: true %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, t(".send_me_reset_password_instructions") %>
  </div>
<% end %>
<%= render "devise/shared/links" %> -->