<div class="layout">
  <div class="wrap">
    <div class="top_margin clearfix">

     <div class="form-modal">  
        <div class="form-toggle">
           <button id="login-toggle" onclick="toggleLogin()">비밀번호를 잊으셨나요?</button>
        </div>
          
        <div id="login-form">
         <%= simple_form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }) do |f| %>
         <%= f.error_notification %>
         <%= f.full_error :confirmation_token %>
            <div class="login_input">    
              <%= f.input :email, required: true, autofocus: true %>
            </div>
            <%= f.button :submit, t(".resend_confirmation_instructions"), class: "btn login" %>
            <p><%= render "devise/shared/links" %></p>
         <% end %>
        </div>
      </div>

    </div>
  </div>
</div>


<!-- <h2><%= t(".resend_confirmation_instructions") %></h2>

<%= simple_form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }) do |f| %>
  <%= f.error_notification %>
  <%= f.full_error :confirmation_token %>

  <div class="form-inputs">
    <%= f.input :email, required: true, autofocus: true %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, t(".resend_confirmation_instructions") %>
  </div>
<% end %>

<%= render "devise/shared/links" %> -->